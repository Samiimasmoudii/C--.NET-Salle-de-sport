@{
    ViewBag.Title = "Choose User";
}

<h2 >Choisir Le type d'utilisateur</h2>

<div >


   
        <div class="row" ">
        <div class="col-md-12 mb-2">
            <a href="@Url.Action("Login", "Auth")" class="btn btn-primary btn-block">Continuer en tant que Membre</a>
        </div>
        <div class="col-md-12 mb-2">
            <a href="@Url.Action("LoginCoach", "Coach")" class="btn btn-primary btn-block">Continuer en tant que Coach</a>
        </div>
        <div class="col-md-12 mb-2">
            <a href="@Url.Action("LoginAdmin", "LoginAdmin")" class="btn btn-primary btn-block">Continuer en tant qu'Admin</a>
        </div>
    </div>
</div>